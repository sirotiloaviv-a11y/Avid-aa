{
  "name": "datascope",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DataScope - a local, browser-only real-time market dashboard for US stocks and crypto, with an alert engine (Hebrew UI)",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs --dir dist",
    "build": "node scripts/build.mjs",
    "test": "node --test \"tests/*.test.js\"",
    "test:e2e": "node tests/e2e/dashboard-flow.mjs",
    "mock": "node scripts/mock-market.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
