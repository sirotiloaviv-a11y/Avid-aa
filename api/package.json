{
  "name": "avid-aa-api",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP API for product data. Dependency-free: Node builtins only.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --disable-warning=ExperimentalWarning --test 'tests/*.test.ts'"
  },
  "dependencies": {},
  "devDependencies": {}
}
